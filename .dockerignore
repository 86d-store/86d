node_modules
.next
.turbo
.86d
# The default context carries only workspace manifests for Turbo prune. Local
# source builds opt into the independent `workspace-modules` named context.
modules/*/**
!modules/*/package.json
.git
.github
.changeset
# Environment files never enter BuildKit context or cache layers.
.env*
*.log
logs/
coverage/
playwright-report/
test-results/
dist/
build/
*.tsbuildinfo
