---
"@86d-app/registry": patch
"db": patch
---

Add fail-closed registry-only Module resolution, transactional cached batch fetching, and curated runtime schema staging for reproducible Store Runtime image builds.
