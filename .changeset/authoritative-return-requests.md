---
"@86d-app/returns": major
---

Add an owner-local authoritative Return request foundation with immutable line
and reason snapshots, row-locked cumulative Order quantity caps, deterministic
operation replay, explicit actor and authority evidence, and an atomic durable
`return.requested@1` fact. Unregister unsafe shopper submission and contain all
Customer/lifecycle routes until verified Store Customer or guest-proof
authorization, restock, refund, Shipping, and reversal workflows are durable.
