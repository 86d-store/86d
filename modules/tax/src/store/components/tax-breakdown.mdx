{props.calculation.totalTax === 0 ? null : (
  <div className="space-y-1 text-sm">
    <div className="flex justify-between text-neutral-600">
      <span>
        Tax ({(props.calculation.effectiveRate * 100).toFixed(1)}%)
        {props.calculation.inclusive ? " (included)" : ""}
      </span>
      <span>{props.formatCurrency(props.calculation.totalTax)}</span>
    </div>
    {props.calculation.shippingTax > 0 && (
      <div className="flex justify-between text-neutral-400 text-xs">
        <span>Includes shipping tax</span>
        <span>{props.formatCurrency(props.calculation.shippingTax)}</span>
      </div>
    )}
  </div>
)}
