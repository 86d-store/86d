# Digital Downloads Module

File delivery via secure, expiring download tokens. Associates downloadable files with products and generates single-use or limited-use tokens for order fulfillment. Supports batch token creation for orders with multiple digital products.

**Scope:** This guide owns Module-specific mechanics. In the 86d.store source checkout, read the repository root [`AGENTS.md`](../../AGENTS.md) for shared rules and gates. In another project, follow that project's instructions and the installed `@86d-store/core` contracts.

## Change protocol

1. **Route.** Read this guide and the Module's [`README.md`](./README.md). In the 86d.store source checkout, storage or cross-Module changes also follow the root [Module contracts](../../AGENTS.md#module-contracts) routes.
2. **Implement** within the Module source shape and patterns below.
3. **Verify.** In the 86d.store source checkout, run `bun run generate:modules` after a Module change, then `bun run generate:modules -- --frozen` and the parent gates. In another project, use that project's checks. Run the Module's focused tests when available.
   - Done when the current project's required checks pass; source-checkout work also requires a passing frozen registry check.

## Structure

```
src/
  index.ts          Factory: digitalDownloads(options?) => Module
  schema.ts         Models: downloadableFile, downloadToken
  service.ts        DigitalDownloadsController interface + types
  service-impl.ts   DigitalDownloadsController implementation
  store/endpoints/
    use-download.ts           GET  /downloads/:token (consume token, get file URL)
    list-my-downloads.ts      GET  /downloads/me (list tokens for authenticated user)
    list-product-files.ts     GET  /downloads/product/:productId (public file metadata)
    store-search.ts           GET  /digital-downloads/store-search
  admin/endpoints/
    list-files.ts             GET    /admin/downloads/files
    create-file.ts            POST   /admin/downloads/files/create
    get-file.ts               GET    /admin/downloads/files/:id
    update-file.ts            PUT    /admin/downloads/files/:id/update
    delete-file.ts            DELETE /admin/downloads/files/:id/delete
    list-tokens.ts            GET    /admin/downloads/tokens
    create-token.ts           POST   /admin/downloads/tokens/create
    create-token-batch.ts     POST   /admin/downloads/tokens/batch
    get-token.ts              GET    /admin/downloads/tokens/:id
    revoke-token.ts           POST   /admin/downloads/tokens/:id/revoke
  __tests__/
    service-impl.test.ts    35 tests (file/token CRUD core)
    controllers.test.ts     32 tests (edge cases, lifecycle)
    endpoint-security.test.ts 16 tests (validation chain, isolation)
    new-features.test.ts    25 tests (getToken, revokeById, batch, updateFile)
```

## Data models

- **downloadableFile**: id, productId, name, url (storage URL), fileSize?, mimeType?, isActive, createdAt, updatedAt
- **downloadToken**: id, token (UUID), fileId, orderId?, email, maxDownloads?, downloadCount, expiresAt?, revokedAt?, createdAt, updatedAt

## Token validation (redeemToken)

Checks in order:

1. Token not found → `{ ok: false, reason: "Token not found" }`
2. `revokedAt` is set → `{ ok: false, reason: "Token revoked" }`
3. `expiresAt < now` → `{ ok: false, reason: "Token expired" }`
4. `downloadCount >= maxDownloads` → `{ ok: false, reason: "Download limit reached" }`
5. Increment `downloadCount`, return `{ ok: true, file, token }`

## Batch token creation

`createTokenBatch({ fileIds, email, orderId?, maxDownloads?, expiresAt? })` creates one token per file ID. All tokens share the same email, orderId, maxDownloads, and expiresAt. Used for order processing with multiple digital products.

## Exports

Types: `DownloadableFile`, `DownloadToken`, `DigitalDownloadsController`

## Patterns

- `/downloads/me` requires authenticated session — email derived from `ctx.context.session.user.email`
- `/downloads/:token` is public (token is the secret) — no session required
- `/downloads/product/:productId` is public — returns only metadata (name, fileSize, mimeType), excludes download URL
- Token value is a UUID generated by `crypto.randomUUID()` — distinct from the record `id`
- `getTokenByValue(token)` searches by the `token` field; `getToken(id)` searches by record `id`
- `revokeTokenById(id)` revokes by record ID; `revokeToken(value)` revokes by token value
- Controller key uses hyphen: `"digital-downloads"` — valid JS bracket notation
- `exactOptionalPropertyTypes` compatible: optional fields use conditional spreads
- The `url` field stores the raw storage URL — the application layer handles redirects or signed URLs
