<div>
  <div className="mb-6">
    <h1 className="font-bold text-2xl text-foreground">
      Digital Downloads
    </h1>
  </div>

  <div className="mb-6 flex gap-1 rounded-lg border border-border bg-muted/50 p-1">
    {["files", "tokens"].map((t) => (
      <button
        key={t}
        type="button"
        onClick={() => props.onTabChange(t)}
        className={`flex-1 rounded-md py-1.5 font-medium text-sm capitalize transition-colors ${
          props.tab === t
            ? "bg-background text-foreground shadow-sm"
            : "text-muted-foreground hover:text-foreground"
        }`}
      >
        {t}
      </button>
    ))}
  </div>

  {props.tabContent}
</div>
