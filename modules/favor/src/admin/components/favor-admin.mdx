<div className="space-y-6">
  <h1 className="font-bold text-2xl text-foreground">Favor</h1>
  {/* Connection status */}
  <div className="rounded-lg border border-border bg-card p-4">
    <div className="flex items-start justify-between gap-4">
      <div className="flex-1">
        <div className="flex items-center gap-2">
          <h3 className="font-medium text-foreground text-sm">Favor Delivery</h3>
          {props.connectionBadge}
        </div>
        {props.credentialRows}
      </div>
    </div>
  </div>

  {/* Stats */}
  {props.statsSection && (
    <div>{props.statsSection}</div>
  )}

  {/* Tabs */}
  <div className="space-y-4">
    <div className="flex gap-1 border-border border-b">
      <button
        type="button"
        onClick={() => props.onTabChange("deliveries")}
        className={`px-4 py-2 text-sm transition-colors ${props.activeTab === "deliveries" ? "border-foreground border-b-2 font-medium text-foreground" : "text-muted-foreground hover:text-foreground"}`}
      >
        Deliveries
      </button>
      <button
        type="button"
        onClick={() => props.onTabChange("areas")}
        className={`px-4 py-2 text-sm transition-colors ${props.activeTab === "areas" ? "border-foreground border-b-2 font-medium text-foreground" : "text-muted-foreground hover:text-foreground"}`}
      >
        Service Areas
      </button>
    </div>

    {props.activeTab === "deliveries" && (
      <div className="space-y-3">
        <div className="flex items-center gap-3">
          <select
            value={props.statusFilter}
            onChange={(e) => props.onStatusFilterChange(e.target.value)}
            className="rounded-md border border-border bg-background px-3 py-1.5 text-foreground text-sm focus:outline-none focus:ring-2 focus:ring-ring"
          >
            <option value="">All Statuses</option>
            <option value="pending">Pending</option>
            <option value="assigned">Assigned</option>
            <option value="en-route">En Route</option>
            <option value="arrived">Arrived</option>
            <option value="completed">Completed</option>
            <option value="cancelled">Cancelled</option>
          </select>
        </div>
        <div className="rounded-lg border border-border bg-card">
          {props.listLoading ? props.deliverySkeleton : props.tableContent}
        </div>
      </div>
    )}

    {props.activeTab === "areas" && (
      <div className="rounded-lg border border-border bg-card">
        {props.areasContent}
      </div>
    )}
  </div>
</div>
