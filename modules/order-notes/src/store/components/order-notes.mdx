<section className="py-6">
  <div className="mb-4">
    <h3 className="font-semibold text-gray-900 text-lg dark:text-gray-100">
      Order Notes{" "}
      <span className="font-normal text-gray-500 text-sm dark:text-gray-400">
        ({props.total})
      </span>
    </h3>
  </div>

  {props.error && (
    <p className="mb-4 rounded-lg bg-red-50 px-3 py-2 text-red-700 text-sm dark:bg-red-950 dark:text-red-300" role="alert">
      {props.error}
    </p>
  )}

  {props.noteCards}

  <div className="mt-4">
    {props.addNoteForm}
  </div>
</section>
