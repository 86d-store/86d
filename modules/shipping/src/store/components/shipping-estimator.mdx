<form onSubmit={props.onSubmit} className="space-y-4 rounded-lg border border-border p-4">
  <div>
    <h3 className="font-display text-sm font-semibold text-foreground">Estimate shipping</h3>
    <p className="mt-0.5 text-xs text-muted-foreground">Enter your country to see available shipping rates.</p>
  </div>

  <div className="grid grid-cols-2 gap-3">
    <div>
      <label htmlFor="shipping-est-country" className="block text-sm font-medium text-foreground mb-1.5">
        Country
      </label>
      <select
        id="shipping-est-country"
        value={props.country}
        onChange={(e) => props.onCountryChange(e.target.value)}
        disabled={props.loading}
        className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm text-foreground focus:border-foreground focus:outline-none focus:ring-1 focus:ring-foreground disabled:opacity-50"
      >
        <option value="US">United States</option>
        <option value="CA">Canada</option>
        <option value="GB">United Kingdom</option>
        <option value="AU">Australia</option>
        <option value="DE">Germany</option>
        <option value="FR">France</option>
        <option value="JP">Japan</option>
        <option value="BR">Brazil</option>
        <option value="MX">Mexico</option>
        <option value="IN">India</option>
      </select>
    </div>
    <div>
      <label htmlFor="shipping-est-amount" className="block text-sm font-medium text-foreground mb-1.5">
        Order total <span className="font-normal text-muted-foreground">(optional)</span>
      </label>
      <div className="relative">
        <span className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-sm text-muted-foreground">$</span>
        <input
          id="shipping-est-amount"
          type="number"
          min="0"
          step="0.01"
          placeholder="0.00"
          value={props.orderAmount}
          onChange={(e) => props.onOrderAmountChange(e.target.value)}
          disabled={props.loading}
          className="w-full rounded-md border border-border bg-background py-2 pr-3 pl-7 text-sm text-foreground placeholder:text-muted-foreground/50 focus:border-foreground focus:outline-none focus:ring-1 focus:ring-foreground disabled:opacity-50"
        />
      </div>
    </div>
  </div>

  <button
    type="submit"
    disabled={props.loading}
    className="w-full rounded-md bg-foreground py-2 text-center text-sm font-medium text-background transition-opacity hover:opacity-85 disabled:opacity-50"
  >
    {props.loading ? "Calculating…" : "Get estimate"}
  </button>

  {props.error && (
    <p className="text-sm text-destructive">{props.error}</p>
  )}

  {props.hasSearched && props.rates.length === 0 && !props.error && (
    <p className="text-sm text-muted-foreground">No shipping options available for this destination.</p>
  )}

  {props.rates.length > 0 && (
    <ul className="divide-y divide-border rounded-md border border-border" role="list">
      {props.rates.map((rate) => (
        <li key={rate.rateId} className="flex items-center justify-between px-3 py-2.5">
          <div>
            <p className="text-sm font-medium text-foreground">{rate.rateName}</p>
            <p className="text-xs text-muted-foreground">{rate.zoneName}</p>
          </div>
          <span className="text-sm font-semibold text-foreground">{rate.formattedPrice}</span>
        </li>
      ))}
    </ul>
  )}
</form>
