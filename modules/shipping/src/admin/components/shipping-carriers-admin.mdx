<div>
  <div className="mb-6 flex items-start justify-between">
    <div>
      <h1 className="font-bold text-2xl text-foreground">Carriers & Methods</h1>
      <p className="mt-1 text-muted-foreground text-sm">
        {props.subtitle}
      </p>
    </div>
    <div className="flex gap-2">
      <button
        type="button"
        onClick={props.onAddMethod}
        className="rounded-md border border-border px-3 py-2 font-medium text-foreground text-sm hover:bg-muted"
      >
        Add method
      </button>
      <button
        type="button"
        onClick={props.onAddCarrier}
        className="rounded-md bg-foreground px-3 py-2 font-medium text-background text-sm hover:opacity-90"
      >
        Add carrier
      </button>
    </div>
  </div>

  {props.content}
</div>
