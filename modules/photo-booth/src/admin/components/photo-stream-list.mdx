<div className="space-y-6">
  {props.error && (
    <div className="rounded-lg bg-destructive/10 p-3 text-destructive text-sm">
      {props.error}
    </div>
  )}

  <div className="grid grid-cols-1 gap-6 lg:grid-cols-3">
    <div className="rounded-lg border border-border bg-card lg:col-span-1">
      <div className="flex items-center justify-between border-border border-b px-5 py-3">
        <h3 className="font-semibold text-foreground text-sm">Streams</h3>
        <button
          type="button"
          onClick={props.onToggleCreate}
          className="rounded bg-foreground px-2.5 py-1 font-medium text-background text-xs hover:opacity-90"
        >
          {props.showCreate ? "Cancel" : "New Stream"}
        </button>
      </div>

      {props.showCreate && (
        <div className="border-border border-b px-5 py-3">
          <label className="mb-1 block font-medium text-muted-foreground text-xs">
            Stream Name
          </label>
          <div className="flex gap-2">
            <input
              type="text"
              value={props.newName}
              onChange={(e) => props.onNewNameChange(e.target.value)}
              className="flex-1 rounded border border-border bg-background px-2.5 py-1.5 text-foreground text-sm"
              placeholder="e.g. Event Photos"
            />
            <button
              type="button"
              onClick={props.onCreate}
              className="rounded bg-foreground px-3 py-1.5 font-medium text-background text-xs hover:opacity-90"
            >
              Create
            </button>
          </div>
        </div>
      )}

      {props.streamListContent}
    </div>

    <div className="rounded-lg border border-border bg-card p-5 lg:col-span-2">
      {props.detailContent}
    </div>
  </div>
</div>
