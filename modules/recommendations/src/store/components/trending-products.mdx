import Link from "next/link";

{props.isLoading ? (
  <section className="py-8">
    <h2 className="mb-4 font-semibold text-foreground text-lg">{props.title}</h2>
    <div className="flex gap-4 overflow-x-auto pb-2">
      {(["k0", "k1", "k2", "k3"]).map((key) => (
        <div key={key} className="w-44 shrink-0 animate-pulse space-y-2">
          <div className="aspect-square rounded-lg bg-muted" />
          <div className="h-4 w-3/4 rounded bg-muted" />
          <div className="h-4 w-1/2 rounded bg-muted" />
        </div>
      ))}
    </div>
  </section>
) : (
  <section className="py-8">
    <h2 className="mb-4 font-semibold text-foreground text-lg">{props.title}</h2>
    <div className="flex gap-4 overflow-x-auto pb-2">
      {props.items?.map((item) => (
        <Link
          key={item.id}
          href={item.href}
          onClick={item.onClick}
          className="group w-44 shrink-0 space-y-2"
        >
          {item.image ? (
            <div className="aspect-square overflow-hidden rounded-lg bg-muted">
              <img
                src={item.image}
                alt={item.name}
                className="h-full w-full object-cover transition-transform group-hover:scale-105"
                loading="lazy"
              />
            </div>
          ) : (
            <div className="flex aspect-square items-center justify-center rounded-lg bg-muted">
              <span className="text-neutral-600 text-xs dark:text-neutral-400">No image</span>
            </div>
          )}
          <p className="truncate font-medium text-foreground text-sm group-hover:underline">
            {item.name}
          </p>
          {item.price && (
            <p className="text-muted-foreground text-sm">{item.price}</p>
          )}
        </Link>
      ))}
    </div>
  </section>
)}
