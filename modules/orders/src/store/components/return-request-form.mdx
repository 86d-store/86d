<form onSubmit={props.onSubmit} className="space-y-4">
  <h3 className="font-semibold text-foreground text-lg">Request a Return</h3>

  {/* Items selection */}
  <div>
    <span className="mb-1.5 block font-medium text-foreground text-sm">
      Items to return
    </span>
    {props.itemChecklistContent}
  </div>

  {/* Reason */}
  <div>
    <label
      htmlFor="return-reason"
      className="mb-1 block font-medium text-foreground text-sm"
    >
      Reason
    </label>
    <select
      id="return-reason"
      value={props.reason}
      onChange={props.onReasonChange}
      className="h-9 w-full rounded-lg border border-border bg-background px-3 text-sm"
    >
      <option value="">Select a reason...</option>
      {props.reasonOptions}
    </select>
  </div>

  {/* Notes */}
  <div>
    <label
      htmlFor="return-notes"
      className="mb-1 block text-muted-foreground text-sm"
    >
      Additional details (optional)
    </label>
    <textarea
      id="return-notes"
      value={props.customerNotes}
      onChange={props.onNotesChange}
      rows={3}
      placeholder="Describe the issue with your order..."
      className="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm placeholder:text-muted-foreground"
    />
  </div>

  {props.error && (
    <p className="text-destructive text-sm" role="alert">
      {props.error}
    </p>
  )}

  <div className="flex gap-2">
    <button
      type="submit"
      disabled={props.submitDisabled}
      className="rounded-lg bg-foreground px-4 py-2 font-medium text-background text-sm transition-opacity hover:bg-foreground/90 disabled:opacity-50"
    >
      {props.submitPending ? "Submitting..." : "Submit Return Request"}
    </button>
    <button
      type="button"
      onClick={props.onCancel}
      className="rounded-lg border border-border px-4 py-2 text-sm transition-colors hover:bg-muted"
    >
      Cancel
    </button>
  </div>
</form>
