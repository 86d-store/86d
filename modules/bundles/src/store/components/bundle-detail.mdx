import Link from "next/link";

{props.loading ? (
  <div className="mx-auto max-w-2xl space-y-6 py-8">
    <div className="h-7 w-1/2 animate-pulse rounded bg-muted" />
    <div className="space-y-2">
      <div className="h-4 w-full animate-pulse rounded bg-muted" />
      <div className="h-4 w-3/4 animate-pulse rounded bg-muted" />
    </div>
    <div className="space-y-3">
      {[1, 2, 3].map((i) => (
        <div key={key} className="flex items-center gap-3 rounded-lg border border-border p-3">
          <div className="size-12 shrink-0 animate-pulse rounded bg-muted" />
          <div className="flex-1 space-y-1.5">
            <div className="h-4 w-1/2 animate-pulse rounded bg-muted" />
            <div className="h-3 w-1/4 animate-pulse rounded bg-muted" />
          </div>
        </div>
      ))}
    </div>
  </div>
) : !props.bundle ? (
  <div className="flex flex-col items-center justify-center py-24 text-center">
    <p className="font-medium text-foreground text-sm">Bundle not found</p>
    <p className="mt-1 text-muted-foreground text-sm">This bundle may have expired or been removed.</p>
    <Link href="/products" className="mt-3 text-muted-foreground text-sm transition-colors hover:text-foreground">
      Browse products
    </Link>
  </div>
) : (
  <div className="mx-auto max-w-2xl py-4 sm:py-6">
    {props.bundle.imageUrl && (
      <div className="aspect-[16/9] overflow-hidden rounded-lg bg-muted">
        <img
          src={props.bundle.imageUrl}
          alt={props.bundle.name}
          className="h-full w-full object-cover"
        />
      </div>
    )}
    <div className="mt-6">
      <div className="flex items-start justify-between gap-3">
        <h1 className="font-display font-semibold text-foreground text-2xl tracking-tight">
          {props.bundle.name}
        </h1>
        <span className="shrink-0 rounded-full bg-green-100 px-3 py-1 font-semibold text-green-800 text-sm dark:bg-green-900/30 dark:text-green-400">
          {props.formatDiscount(props.bundle.discountType, props.bundle.discountValue)}
        </span>
      </div>
      {props.bundle.description && (
        <p className="mt-3 text-muted-foreground text-sm leading-relaxed">
          {props.bundle.description}
        </p>
      )}
    </div>

    <div className="mt-8">
      <h2 className="mb-3 font-semibold text-foreground text-sm">What's Included</h2>
      <div className="divide-y divide-border rounded-lg border border-border">
        {props.bundle.items.map((item) => (
          <div key={item.id} className="flex items-center gap-3 px-4 py-3">
            {item.productImageUrl ? (
              <img
                src={item.productImageUrl}
                alt={item.productName ?? item.productId}
                className="size-12 shrink-0 rounded-md object-cover"
              />
            ) : (
              <div className="flex size-12 shrink-0 items-center justify-center rounded-md bg-muted">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" className="text-muted-foreground/40" aria-hidden="true">
                  <rect width="18" height="18" x="3" y="3" rx="2" />
                  <circle cx="9" cy="9" r="2" />
                  <path d="m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21" />
                </svg>
              </div>
            )}
            <div className="min-w-0 flex-1">
              {item.productSlug ? (
                <Link
                  href={`/products/${item.productSlug}`}
                  className="font-medium text-foreground text-sm transition-colors hover:text-foreground/80"
                >
                  {item.productName ?? item.productId}
                </Link>
              ) : (
                <p className="font-medium text-foreground text-sm">
                  {item.productName ?? item.productId}
                </p>
              )}
              {item.variantId && (
                <p className="text-muted-foreground text-xs">Variant: {item.variantId}</p>
              )}
            </div>
            <span className="shrink-0 text-muted-foreground text-sm">×{item.quantity}</span>
          </div>
        ))}
      </div>
    </div>
  </div>
)}
