{props.success ? (
  <div className="rounded-xl border border-emerald-200 bg-emerald-50 p-5 text-center dark:border-emerald-800 dark:bg-emerald-950/30">
    <p className="font-semibold text-emerald-800 dark:text-emerald-200">
      You're subscribed!
    </p>
    <p className="mt-1 text-emerald-700 text-sm dark:text-emerald-300">
      Thank you for subscribing. We'll keep you in the loop.
    </p>
  </div>
) : props.compact ? (
  <form onSubmit={props.onSubmit} className="space-y-2">
    {props.title && (
      <p className="font-medium text-foreground text-sm">{props.title}</p>
    )}
    {props.description && (
      <p className="text-muted-foreground text-xs">{props.description}</p>
    )}
    <div className="flex gap-2">
      <input
        type="email"
        required
        value={props.email}
        onChange={(e) => props.onEmailChange(e.target.value)}
        placeholder="you@example.com"
        className="min-w-0 flex-1 rounded-lg border border-input bg-background px-3 py-2 text-sm outline-none ring-offset-background focus:ring-2 focus:ring-ring focus:ring-offset-1"
      />
      <button
        type="submit"
        disabled={props.isLoading}
        className="shrink-0 rounded-lg bg-primary px-4 py-2 font-medium text-primary-foreground text-sm transition-opacity disabled:opacity-60"
      >
        {props.isLoading ? "…" : "Subscribe"}
      </button>
    </div>
    {props.error && (
      <p className="text-destructive text-xs" role="alert">
        {props.error}
      </p>
    )}
  </form>
) : (
  <form
    onSubmit={props.onSubmit}
    className="space-y-4 rounded-xl border border-border bg-muted/30 p-6"
  >
    {props.title && (
      <h3 className="font-semibold text-foreground text-lg">{props.title}</h3>
    )}
    {props.description && (
      <p className="text-muted-foreground text-sm">{props.description}</p>
    )}

    {props.showName && (
      <div className="grid gap-4 sm:grid-cols-2">
        <div>
          <label
            htmlFor="nl-first-name"
            className="mb-1 block font-medium text-foreground text-sm"
          >
            First name
          </label>
          <input
            id="nl-first-name"
            type="text"
            maxLength={200}
            value={props.firstName}
            onChange={(e) => props.onFirstNameChange(e.target.value)}
            placeholder="Jane"
            className="w-full rounded-lg border border-input bg-background px-3 py-2 text-sm outline-none ring-offset-background focus:ring-2 focus:ring-ring focus:ring-offset-1"
          />
        </div>
        <div>
          <label
            htmlFor="nl-last-name"
            className="mb-1 block font-medium text-foreground text-sm"
          >
            Last name
          </label>
          <input
            id="nl-last-name"
            type="text"
            maxLength={200}
            value={props.lastName}
            onChange={(e) => props.onLastNameChange(e.target.value)}
            placeholder="Doe"
            className="w-full rounded-lg border border-input bg-background px-3 py-2 text-sm outline-none ring-offset-background focus:ring-2 focus:ring-ring focus:ring-offset-1"
          />
        </div>
      </div>
    )}

    <div>
      <label
        htmlFor="nl-email"
        className="mb-1 block font-medium text-foreground text-sm"
      >
        Email <span className="text-destructive">*</span>
      </label>
      <input
        id="nl-email"
        type="email"
        required
        value={props.email}
        onChange={(e) => props.onEmailChange(e.target.value)}
        placeholder="you@example.com"
        className="w-full rounded-lg border border-input bg-background px-3 py-2 text-sm outline-none ring-offset-background focus:ring-2 focus:ring-ring focus:ring-offset-1"
      />
    </div>

    {props.error && (
      <p className="text-destructive text-sm" role="alert">
        {props.error}
      </p>
    )}

    <button
      type="submit"
      disabled={props.isLoading}
      className="rounded-lg bg-primary px-5 py-2 font-medium text-primary-foreground text-sm transition-opacity disabled:opacity-60"
    >
      {props.isLoading ? "Subscribing…" : "Subscribe"}
    </button>
  </form>
)}
