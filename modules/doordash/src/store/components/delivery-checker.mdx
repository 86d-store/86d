<div className="rounded-xl border border-border/60 bg-muted/20 p-5">
  <div className="mb-4 flex items-center gap-2.5">
    <div className="flex h-8 w-8 items-center justify-center rounded-full bg-foreground/10">
      <svg
        xmlns="http://www.w3.org/2000/svg"
        width="16"
        height="16"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="1.75"
        strokeLinecap="round"
        strokeLinejoin="round"
        className="text-foreground"
        aria-hidden="true"
      >
        <path d="M20 10c0 4.993-5.539 10.193-7.399 11.799a1 1 0 0 1-1.202 0C9.539 20.193 4 14.993 4 10a8 8 0 0 1 16 0" />
        <circle cx="12" cy="10" r="3" />
      </svg>
    </div>
    <div>
      <p className="font-semibold text-foreground text-sm">DoorDash delivery</p>
      <p className="text-muted-foreground text-xs">Check if we deliver to your location</p>
    </div>
  </div>

  {!props.hasResult && !props.isChecking && !props.error && (
    <button
      type="button"
      onClick={props.onLocate}
      className="flex w-full items-center justify-center gap-2 rounded-lg bg-foreground px-4 py-2.5 font-semibold text-background text-sm transition-all hover:opacity-90 active:scale-[0.98]"
    >
      <svg
        xmlns="http://www.w3.org/2000/svg"
        width="14"
        height="14"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
        aria-hidden="true"
      >
        <line x1="2" x2="5" y1="12" y2="12" />
        <line x1="19" x2="22" y1="12" y2="12" />
        <line x1="12" x2="12" y1="2" y2="5" />
        <line x1="12" x2="12" y1="19" y2="22" />
        <circle cx="12" cy="12" r="7" />
        <circle cx="12" cy="12" r="3" />
      </svg>
      Use my location
    </button>
  )}

  {props.isChecking && (
    <div className="flex items-center justify-center gap-2 py-4 text-muted-foreground text-sm">
      <svg
        className="h-4 w-4 animate-spin"
        xmlns="http://www.w3.org/2000/svg"
        fill="none"
        viewBox="0 0 24 24"
        aria-hidden="true"
      >
        <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
        <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z" />
      </svg>
      Checking availability…
    </div>
  )}

  {!props.isChecking && props.error && (
    <div className="rounded-lg border border-destructive/20 bg-destructive/5 p-3">
      <p className="text-destructive text-sm">{props.error}</p>
      <button
        type="button"
        onClick={props.onLocate}
        className="mt-2 text-muted-foreground text-xs underline underline-offset-2 transition-colors hover:text-foreground"
      >
        Try again
      </button>
    </div>
  )}

  {!props.isChecking && props.hasResult && !props.error && props.result && (
    <div>
      {props.result.available ? (
        <div className="rounded-lg border border-emerald-500/30 bg-emerald-500/5 p-4">
          <div className="mb-2 flex items-center gap-2">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="16"
              height="16"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              strokeWidth="2.5"
              strokeLinecap="round"
              strokeLinejoin="round"
              className="text-emerald-600 dark:text-emerald-400"
              aria-hidden="true"
            >
              <circle cx="12" cy="12" r="10" />
              <path d="m9 12 2 2 4-4" />
            </svg>
            <p className="font-semibold text-emerald-700 text-sm dark:text-emerald-400">
              DoorDash delivers to you!
            </p>
          </div>
          <div className="grid grid-cols-2 gap-2">
            {props.result.estimatedMinutes != null && (
              <div className="rounded-md bg-emerald-500/10 px-3 py-2 text-center">
                <p className="font-bold text-emerald-700 text-sm tabular-nums dark:text-emerald-400">
                  {props.result.estimatedMinutes} min
                </p>
                <p className="text-emerald-600/70 text-xs dark:text-emerald-500">est. delivery</p>
              </div>
            )}
            {props.result.deliveryFee != null && (
              <div className="rounded-md bg-emerald-500/10 px-3 py-2 text-center">
                <p className="font-bold text-emerald-700 text-sm tabular-nums dark:text-emerald-400">
                  {props.formatCurrency(props.result.deliveryFee, props.currency)}
                </p>
                <p className="text-emerald-600/70 text-xs dark:text-emerald-500">delivery fee</p>
              </div>
            )}
          </div>
        </div>
      ) : (
        <div className="rounded-lg border border-border bg-muted/30 p-4">
          <div className="flex items-center gap-2 text-muted-foreground">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="16"
              height="16"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              strokeWidth="2"
              strokeLinecap="round"
              strokeLinejoin="round"
              aria-hidden="true"
            >
              <circle cx="12" cy="12" r="10" />
              <path d="m4.9 4.9 14.2 14.2" />
            </svg>
            <p className="text-sm">DoorDash delivery is not available in your area yet.</p>
          </div>
          <button
            type="button"
            onClick={props.onLocate}
            className="mt-2 text-muted-foreground text-xs underline underline-offset-2 transition-colors hover:text-foreground"
          >
            Check a different location
          </button>
        </div>
      )}
    </div>
  )}

  {!props.isChecking && props.hasResult && !props.error && !props.result && (
    <div className="rounded-lg border border-border bg-muted/30 p-4">
      <p className="text-muted-foreground text-sm">
        DoorDash delivery is not available in your area yet.
      </p>
      <button
        type="button"
        onClick={props.onLocate}
        className="mt-2 text-muted-foreground text-xs underline underline-offset-2 transition-colors hover:text-foreground"
      >
        Check a different location
      </button>
    </div>
  )}
</div>
