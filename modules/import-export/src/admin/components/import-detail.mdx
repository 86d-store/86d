<div className="space-y-6">
  <div className="flex items-center justify-between">
    <button
      type="button"
      onClick={props.onBack}
      className="text-muted-foreground text-sm hover:text-foreground"
    >
      &larr; Back to list
    </button>
    <span className={props.statusClassName}>
      {props.status}
    </span>
  </div>

  {props.detailContent}

  {props.errorContent}
</div>
