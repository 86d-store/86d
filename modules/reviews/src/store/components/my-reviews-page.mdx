<div>
  <div className="mb-6 flex items-center justify-between">
    <h2 className="font-semibold text-foreground text-lg">Your Reviews</h2>
    <span className="text-muted-foreground text-sm">
      {props.reviewCount} {props.reviewCount === 1 ? "review" : "reviews"}
    </span>
  </div>

  {props.reviewCount === 0 ? (
    <div className="py-16 text-center">
      <p className="text-4xl">⭐</p>
      <h3 className="mt-4 font-semibold text-foreground text-base">
        No reviews yet
      </h3>
      <p className="mt-2 text-muted-foreground text-sm">
        Purchase a product and share your experience.
      </p>
    </div>
  ) : (
    props.reviewsContent
  )}

  {props.pagination}
</div>
