<span
  role="img"
  className={props.sizeClass}
  aria-label={props.ariaLabel}
>
  {[1, 2, 3, 4, 5].map((n) => (
    <span
      key={n}
      className={
        n <= props.filledCount
          ? "text-amber-400"
          : "text-gray-200 dark:text-gray-700"
      }
    >
      ★
    </span>
  ))}
</span>
