<div className="rounded-xl border border-gray-200 bg-white p-6 dark:border-gray-800 dark:bg-gray-900">
  <div className="flex items-center justify-between">
    <span className="font-medium text-gray-500 text-sm dark:text-gray-400">Tier Progress</span>
    {props.multiplier && (
      <span className={`inline-flex items-center rounded-full px-2 py-0.5 font-medium text-xs ${props.tierBg} ${props.tierText}`}>
        {props.multiplier}
      </span>
    )}
  </div>
  <div className="mt-3">
    <div className="h-2.5 w-full overflow-hidden rounded-full bg-gray-100 dark:bg-gray-800">
      <div
        className="h-full rounded-full bg-gradient-to-r from-amber-400 via-yellow-400 to-indigo-500 transition-all duration-500"
        style={{ width: `${props.progressPercent}%` }}
      />
    </div>
    <p className="mt-2 text-gray-500 text-xs dark:text-gray-400">{props.progressLabel}</p>
  </div>
  {props.tierSteps}
</div>
