<div className="inline-flex flex-col gap-2">
  {props.success ? (
    <div className="rounded-md border border-green-200 bg-green-50 px-4 py-2.5 text-green-700 text-sm dark:border-green-800 dark:bg-green-950 dark:text-green-400">
      Backorder placed successfully! We will notify you when the item is available.
    </div>
  ) : (
    <>
      {props.maxQuantityPerOrder && props.maxQuantityPerOrder > 1 && (
        <div className="flex items-center gap-2">
          <label htmlFor="backorder-qty" className="text-muted-foreground text-sm">
            Qty:
          </label>
          <select
            id="backorder-qty"
            value={props.quantity}
            onChange={(e) => props.onQuantityChange(Number(e.target.value))}
            className="rounded-md border border-gray-200 px-2 py-1 text-sm dark:border-gray-700 dark:bg-gray-900"
          >
            {Array.from({ length: props.maxQuantityPerOrder }, (_, i) => i + 1).map((n) => (
              <option key={n} value={n}>
                {n}
              </option>
            ))}
          </select>
        </div>
      )}
      <button
        type="button"
        onClick={props.onBackorder}
        disabled={props.isPending || !props.eligible}
        className="inline-flex items-center justify-center gap-2 rounded-md border border-amber-300 bg-amber-50 px-4 py-2 font-medium text-amber-800 text-sm transition-colors hover:bg-amber-100 disabled:opacity-50 dark:border-amber-700 dark:bg-amber-950 dark:text-amber-300 dark:hover:bg-amber-900"
      >
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" className="h-4 w-4">
          <path d="M10 3.75a2 2 0 10-4 0 2 2 0 004 0zM17.25 4.5a.75.75 0 000-1.5h-5.5a.75.75 0 000 1.5h5.5zM5 3.75a.75.75 0 01-.75.75H2.75a.75.75 0 010-1.5h1.5A.75.75 0 015 3.75zM4.25 17a.75.75 0 01.75-.75h12.25a.75.75 0 010 1.5H5a.75.75 0 01-.75-.75zM3.25 12a.75.75 0 000 1.5h5.5a.75.75 0 000-1.5h-5.5zM14 12.25a2 2 0 10-4 0 2 2 0 004 0zM17.25 12.75a.75.75 0 000-1.5h-1.5a.75.75 0 000 1.5h1.5z" />
        </svg>
        {props.isPending ? "Placing Backorder..." : "Backorder"}
      </button>
      {props.estimatedLeadDays && (
        <p className="text-muted-foreground text-xs">
          Estimated availability: ~{props.estimatedLeadDays} {props.estimatedLeadDays === 1 ? "day" : "days"}
        </p>
      )}
      {props.policyMessage && (
        <p className="text-muted-foreground text-xs">{props.policyMessage}</p>
      )}
    </>
  )}
  {props.error && <p className="text-red-600 text-xs">{props.error}</p>}
</div>
