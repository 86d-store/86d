<div className="space-y-5">
  <div>
    <h2 className="font-display text-lg font-semibold text-foreground">Payment</h2>
  </div>

  {props.loading && (
    <div className="flex flex-col items-center justify-center gap-3 py-10">
      <div className="h-6 w-6 animate-spin rounded-full border-2 border-border border-t-foreground" />
      <p className="text-muted-foreground text-sm">Processing payment…</p>
    </div>
  )}

  {!props.loading && props.error && (
    <div className="rounded-md border border-destructive/30 bg-destructive/5 p-4">
      <p className="text-destructive text-sm">{props.error}</p>
      <button
        type="button"
        onClick={props.onRetry}
        className="mt-3 rounded-md bg-foreground px-4 py-2 font-medium text-background text-sm transition-opacity hover:opacity-85"
      >
        Try again
      </button>
    </div>
  )}

  {!props.loading && !props.error && props.payment && (
    <>
      {props.payment.status === "succeeded" ? (
        <div className="rounded-md border border-green-500/30 bg-green-500/5 p-4">
          <p className="font-medium text-green-700 text-sm dark:text-green-400">
            Payment of {props.total} confirmed
          </p>
          <p className="mt-1 text-muted-foreground text-xs">
            Redirecting to order review…
          </p>
        </div>
      ) : props.paymentProvider === "stripe" ? (
        <div className="space-y-4">
          <div className="rounded-md border border-border bg-muted/20 p-4">
            <div className="flex items-center justify-between">
              <p className="font-medium text-foreground text-sm">Stripe payment</p>
              <span className="rounded-full bg-amber-500/10 px-2 py-0.5 font-medium text-amber-700 text-xs dark:text-amber-400">
                Awaiting card details
              </span>
            </div>
            <p className="mt-1 text-muted-foreground text-sm">
              Amount due: <span className="font-semibold text-foreground">{props.total}</span>
            </p>
          </div>
          <p className="text-muted-foreground text-xs">
            Your Stripe payment form is rendered by the store checkout page. If you are building a custom template, mount a Stripe Elements form here and confirm the payment intent.
          </p>
        </div>
      ) : props.paymentProvider === "paypal" ? (
        <div className="space-y-4">
          <div className="rounded-md border border-border bg-muted/20 p-4">
            <div className="flex items-center justify-between">
              <p className="font-medium text-foreground text-sm">PayPal payment</p>
              <span className="rounded-full bg-amber-500/10 px-2 py-0.5 font-medium text-amber-700 text-xs dark:text-amber-400">
                Awaiting approval
              </span>
            </div>
            <p className="mt-1 text-muted-foreground text-sm">
              Amount due: <span className="font-semibold text-foreground">{props.total}</span>
            </p>
          </div>
          <p className="text-muted-foreground text-xs">
            Your PayPal Buttons component is rendered by the store checkout page. If you are building a custom template, mount the PayPal Buttons SDK here and capture the order.
          </p>
        </div>
      ) : props.paymentProvider === "braintree" ? (
        <div className="space-y-4">
          <div className="rounded-md border border-border bg-muted/20 p-4">
            <div className="flex items-center justify-between">
              <p className="font-medium text-foreground text-sm">Braintree payment</p>
              <span className="rounded-full bg-amber-500/10 px-2 py-0.5 font-medium text-amber-700 text-xs dark:text-amber-400">
                Awaiting card details
              </span>
            </div>
            <p className="mt-1 text-muted-foreground text-sm">
              Amount due: <span className="font-semibold text-foreground">{props.total}</span>
            </p>
          </div>
          <p className="text-muted-foreground text-xs">
            Your Braintree Drop-in UI is rendered by the store checkout page. If you are building a custom template, mount the Braintree Drop-in here and submit the nonce.
          </p>
        </div>
      ) : props.paymentProvider === "square" ? (
        <div className="space-y-4">
          <div className="rounded-md border border-border bg-muted/20 p-4">
            <div className="flex items-center justify-between">
              <p className="font-medium text-foreground text-sm">Square payment</p>
              <span className="rounded-full bg-amber-500/10 px-2 py-0.5 font-medium text-amber-700 text-xs dark:text-amber-400">
                Awaiting card details
              </span>
            </div>
            <p className="mt-1 text-muted-foreground text-sm">
              Amount due: <span className="font-semibold text-foreground">{props.total}</span>
            </p>
          </div>
          <p className="text-muted-foreground text-xs">
            Your Square Web Payments form is rendered by the store checkout page. If you are building a custom template, mount the Square Web Payments SDK here.
          </p>
        </div>
      ) : (
        <div className="rounded-md border border-border bg-muted/30 p-4">
          <p className="text-muted-foreground text-sm">
            Payment status: {props.payment.status}
          </p>
        </div>
      )}
    </>
  )}

  {!props.loading && (
    <div className="flex items-center gap-3">
      <button
        type="button"
        onClick={props.onBack}
        className="rounded-md border border-border px-4 py-2.5 text-foreground text-sm transition-colors hover:bg-muted"
      >
        Back
      </button>
    </div>
  )}
</div>
