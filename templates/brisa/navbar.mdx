import Link from "next/link";

<header
  className={`sticky top-0 z-50 w-full transition-[background-color,border-color,backdrop-filter] duration-300 ${
    props.scrolled
      ? "border-border/40 border-b bg-background/80 backdrop-blur-2xl backdrop-saturate-150"
      : "border-transparent border-b bg-transparent"
  }`}
>
  <div className="mx-auto flex h-16 max-w-7xl items-center px-4 sm:px-6 lg:px-8">
    {/* Logo */}
    <Link
      href="/"
      className="flex shrink-0 items-center gap-2.5 transition-opacity hover:opacity-70"
    >
      <img
        src={props.logoLight}
        alt={props.storeName}
        className="h-6 w-auto dark:hidden"
      />
      <img
        src={props.logoDark}
        alt={props.storeName}
        className="hidden h-6 w-auto dark:block"
      />
      <span className="font-display font-semibold text-foreground text-sm tracking-tight">
        {props.storeName}
      </span>
    </Link>

    {/* Desktop navigation */}
    <nav aria-label="Main navigation" className="ml-auto hidden items-center gap-1 md:flex lg:ml-10">
      {props.navItems.map((link) => (
        <Link
          key={link.label}
          href={link.href}
          className="rounded-md px-3 py-1.5 text-muted-foreground text-sm transition-colors hover:text-foreground"
        >
          {link.label}
        </Link>
      ))}
    </nav>

    {/* Right actions */}
    <div className="ml-auto flex items-center gap-0.5 md:ml-6">
      {/* Search (command menu) */}
      <StoreSearchCommand />

      {/* Account */}
      <Link
        href="/account"
        className="hidden size-9 items-center justify-center rounded-lg text-muted-foreground transition-colors hover:text-foreground sm:flex"
        aria-label="My account"
      >
        <span className="sr-only">Account</span>
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width="18"
          height="18"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="1.5"
          strokeLinecap="round"
          strokeLinejoin="round"
          aria-hidden="true"
        >
          <circle cx="12" cy="8" r="5" />
          <path d="M20 21a8 8 0 0 0-16 0" />
        </svg>
      </Link>

      {/* Theme toggle — parent handles `sm:` breakpoint; children use only `dark:` display
          (avoids compound `dark:sm:flex`, which may be absent if MDX is not scanned). */}
      <div className="hidden items-center gap-0.5 sm:flex">
        <button
          type="button"
          onClick={() => props.setTheme("light")}
          className="hidden size-9 items-center justify-center rounded-lg text-muted-foreground transition-colors hover:text-foreground dark:flex"
          aria-label="Switch to light mode"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="18"
            height="18"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            strokeWidth="1.5"
            strokeLinecap="round"
            strokeLinejoin="round"
            aria-hidden="true"
          >
            <circle cx="12" cy="12" r="4" />
            <path d="M12 2v2" />
            <path d="M12 20v2" />
            <path d="m4.93 4.93 1.41 1.41" />
            <path d="m17.66 17.66 1.41 1.41" />
            <path d="M2 12h2" />
            <path d="M20 12h2" />
            <path d="m6.34 17.66-1.41 1.41" />
            <path d="m19.07 4.93-1.41 1.41" />
          </svg>
        </button>
        <button
          type="button"
          onClick={() => props.setTheme("dark")}
          className="flex size-9 items-center justify-center rounded-lg text-muted-foreground transition-colors hover:text-foreground dark:hidden"
          aria-label="Switch to dark mode"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="18"
            height="18"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            strokeWidth="1.5"
            strokeLinecap="round"
            strokeLinejoin="round"
            aria-hidden="true"
          >
            <path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z" />
          </svg>
        </button>
      </div>

      {/* Additional actions (e.g., CartButton from MDX) */}
      {props.actions}

      {/* Mobile menu button */}
      <button
        type="button"
        className="flex size-9 items-center justify-center rounded-lg text-muted-foreground transition-colors hover:text-foreground md:hidden"
        onClick={props.toggleMenu}
        aria-label={props.isOpen ? "Close menu" : "Open menu"}
        aria-expanded={props.isOpen}
      >
        <div className="relative h-4 w-4">
          <span
            aria-hidden="true"
            className={`absolute left-0 block h-px w-4 bg-current transition-all duration-200 ${
              props.isOpen ? "top-1/2 -translate-y-1/2 rotate-45" : "top-1"
            }`}
          />
          <span
            aria-hidden="true"
            className={`absolute top-1/2 left-0 block h-px w-4 -translate-y-1/2 bg-current transition-opacity duration-150 ${
              props.isOpen ? "opacity-0" : ""
            }`}
          />
          <span
            aria-hidden="true"
            className={`absolute left-0 block h-px w-4 bg-current transition-all duration-200 ${
              props.isOpen ? "top-1/2 -translate-y-1/2 -rotate-45" : "top-3"
            }`}
          />
        </div>
      </button>
    </div>
  </div>

  {/* Mobile menu overlay */}
  <div
    className={`fixed inset-0 top-16 z-40 flex flex-col bg-background transition-all duration-300 md:hidden ${
      props.isOpen
        ? "pointer-events-auto opacity-100"
        : "pointer-events-none opacity-0"
    }`}
  >
    <nav aria-label="Mobile navigation" className="flex flex-1 flex-col px-6 pt-6">
      {props.navItems.map((link) => (
        <Link
          key={link.label}
          href={link.href}
          onClick={props.handleNavClick}
          className="border-border/30 border-b py-4 font-medium text-foreground text-lg transition-colors hover:text-muted-foreground"
        >
          {link.label}
        </Link>
      ))}

      <div className="mt-6 flex flex-col gap-3">
        <Link
          href="/account"
          onClick={props.handleNavClick}
          className="flex items-center gap-3 text-muted-foreground text-sm transition-colors hover:text-foreground"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="16"
            height="16"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            strokeWidth="1.5"
            aria-hidden="true"
          >
            <circle cx="12" cy="8" r="5" />
            <path d="M20 21a8 8 0 0 0-16 0" />
          </svg>
          Account
        </Link>
        <button
          type="button"
          onClick={() => props.setTheme("light")}
          className="hidden items-center gap-3 text-muted-foreground text-sm transition-colors hover:text-foreground dark:flex"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="16"
            height="16"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            strokeWidth="1.5"
            aria-hidden="true"
          >
            <circle cx="12" cy="12" r="4" />
            <path d="M12 2v2" />
            <path d="M12 20v2" />
            <path d="m4.93 4.93 1.41 1.41" />
            <path d="m17.66 17.66 1.41 1.41" />
            <path d="M2 12h2" />
            <path d="M20 12h2" />
            <path d="m6.34 17.66-1.41 1.41" />
            <path d="m19.07 4.93-1.41 1.41" />
          </svg>
          Light mode
        </button>
        <button
          type="button"
          onClick={() => props.setTheme("dark")}
          className="flex items-center gap-3 text-muted-foreground text-sm transition-colors hover:text-foreground dark:hidden"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="16"
            height="16"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            strokeWidth="1.5"
            aria-hidden="true"
          >
            <path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z" />
          </svg>
          Dark mode
        </button>
      </div>
    </nav>
  </div>
</header>
