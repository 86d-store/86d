import { ArrowUpRight, Menu, UserRound, X } from "lucide-react";
import { cn } from "@86d-store/ui/lib/utils";
import { Separator } from "@86d-store/ui/shadcn/separator";
import { Sheet, SheetClose, SheetContent, SheetDescription, SheetHeader, SheetTitle, SheetTrigger } from "@86d-store/ui/shadcn/sheet";
import { Text } from "@86d-store/ui/text";
import { View } from "@86d-store/ui/view";
import Link from "next/link";
import Container from "./_components/container.mdx";
import ThemeControl from "./_components/theme-control.mdx";

<header className="sticky top-0 z-40 w-full border-border border-b bg-background/95 backdrop-blur-sm">
  <Container className="flex h-18 items-center gap-4">
    <Link href="/" className="flex min-h-11 min-w-0 items-center gap-3 rounded-md outline-none transition-opacity duration-150 hover:opacity-70 focus-visible:ring-2 focus-visible:ring-ring">
      <img src={props.logoLight} alt="" className="h-6 w-auto shrink-0 dark:hidden" />
      <img src={props.logoDark} alt="" className="hidden h-6 w-auto shrink-0 dark:block" />
      <Text className="max-w-36 truncate font-display font-semibold text-sm tracking-tight sm:max-w-48 lg:max-w-56">{props.storeName}</Text>
    </Link>

    <nav aria-label="Main navigation" className="ml-auto hidden items-center gap-1 lg:flex">
      {props.navItems.map((link) => (
        <Link key={link.href} href={link.href} aria-current={link.active ? "page" : undefined} className={cn("inline-flex min-h-10 items-center rounded-md px-3 text-sm outline-none transition-colors duration-150 hover:bg-muted hover:text-foreground focus-visible:ring-2 focus-visible:ring-ring", link.active ? "bg-muted font-medium text-foreground" : "text-muted-foreground")}>
          {link.label}
        </Link>
      ))}
    </nav>

    <View className="ml-auto flex shrink-0 items-center gap-0.5 lg:ml-3">
      <StoreSearchCommand />
      <StoreLink href="/account" variant="ghost" size="icon" aria-label="My account" className="hidden sm:inline-flex">
        <UserRound aria-hidden="true" />
      </StoreLink>
      <View className="hidden sm:block">
        <ThemeControl onLight={props.handleLightTheme} onDark={props.handleDarkTheme} />
      </View>
      {props.actions}
      <Sheet open={props.isOpen} onOpenChange={props.onOpenChange}>
        <SheetTrigger render={<StoreAction static variant="ghost" size="icon" className="lg:hidden" aria-label="Open menu" />}>
          <Menu aria-hidden="true" />
        </SheetTrigger>
        <SheetContent side="right" showCloseButton={false} className="w-full max-w-sm gap-0 overflow-y-auto transition-[translate,opacity] duration-150 ease-out motion-reduce:transition-none">
          <SheetHeader className="flex-row items-start justify-between gap-4 p-6">
            <View className="flex min-w-0 flex-col gap-1">
              <SheetTitle className="truncate">{props.storeName}</SheetTitle>
              <SheetDescription>Explore the store</SheetDescription>
            </View>
            <SheetClose render={<StoreAction static variant="ghost" size="icon" aria-label="Close menu" />}>
              <X aria-hidden="true" />
            </SheetClose>
          </SheetHeader>
          <Separator />
          <nav aria-label="Mobile navigation" className="flex flex-col gap-1 p-4">
            {props.navItems.map((link) => (
              <Link key={link.href} href={link.href} onClick={props.handleNavClick} aria-current={link.active ? "page" : undefined} className={cn("flex min-h-14 items-center justify-between gap-4 rounded-lg px-3 font-display text-lg outline-none transition-colors duration-150 hover:bg-muted focus-visible:ring-2 focus-visible:ring-ring", link.active && "bg-muted font-medium")}>
                {link.label}
                <ArrowUpRight size={20} aria-hidden="true" className="text-muted-foreground" />
              </Link>
            ))}
          </nav>
          <View className="mt-auto flex flex-col gap-4 p-6">
            <Separator />
            <View className="flex items-center justify-between gap-4">
              <StoreLink href="/account" variant="ghost" onClick={props.handleNavClick}>
                <UserRound data-icon="inline-start" aria-hidden="true" />
                {"My account"}
              </StoreLink>
              <ThemeControl onLight={props.handleLightTheme} onDark={props.handleDarkTheme} />
            </View>
            <Link href="/track" onClick={props.handleNavClick} className="inline-flex min-h-11 items-center rounded-md px-3 text-sm text-muted-foreground underline-offset-4 outline-none hover:underline focus-visible:ring-2 focus-visible:ring-ring">Track an order</Link>
          </View>
        </SheetContent>
      </Sheet>
    </View>
  </Container>
</header>
