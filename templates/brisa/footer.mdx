import { Text } from "@86d-app/ui/text";
import { View } from "@86d-app/ui/view";
import Link from "next/link";
import Container from "./_components/container.mdx";

<footer className="border-border border-t bg-muted/30">
  <Container className="grid gap-10 py-12 sm:py-16 lg:grid-cols-8 lg:gap-12">
    <View className="flex flex-col gap-4 lg:col-span-3">
      <Text variant="h2" className="text-balance font-display font-medium text-2xl tracking-tight">A note from the store.</Text>
      <Text variant="p" className="max-w-sm text-pretty text-sm leading-relaxed text-muted-foreground">Sign up for product news and store updates.</Text>
      <View className="w-full max-w-sm">
        <NewsletterForm compact={true} title="" description="" />
      </View>
    </View>
    <nav aria-label="Footer navigation" className="grid grid-cols-2 gap-x-6 gap-y-8 sm:grid-cols-4 lg:col-span-5">
      {props.sections.map((section) => (
        <View key={section.title} className="flex flex-col gap-3">
          <Text variant="h3" className="font-medium text-sm">{section.title}</Text>
          <ul className="flex flex-col">
            {section.links.map((link) => (
              <li key={link.href}>
                <Link href={link.href} className="inline-flex min-h-11 items-center rounded-sm text-sm text-muted-foreground underline-offset-4 outline-none transition-colors duration-150 hover:text-foreground hover:underline focus-visible:ring-2 focus-visible:ring-ring sm:min-h-10">{link.name}</Link>
              </li>
            ))}
          </ul>
        </View>
      ))}
    </nav>
  </Container>
  <Container className="flex flex-col gap-5 border-border border-t py-6 sm:flex-row sm:items-center sm:justify-between">
    <Link href={props.logo.url} className="inline-flex min-h-11 w-fit items-center gap-3 rounded-md outline-none focus-visible:ring-2 focus-visible:ring-ring">
      <img src={props.logo.lightSrc} alt="" className="h-5 w-auto dark:hidden" />
      <img src={props.logo.darkSrc} alt="" className="hidden h-5 w-auto dark:block" />
      <Text className="font-display font-medium text-sm">{props.storeName}</Text>
    </Link>
    <Text variant="p" className="text-xs tabular-nums text-muted-foreground">© {props.year} {props.storeName}</Text>
  </Container>
</footer>
